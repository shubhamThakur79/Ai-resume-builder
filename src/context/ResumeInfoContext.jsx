import { createContext } from "react";

export const ResumeInfoContext = createContext({})
export const ResumeUpdatedContext = createContext({})
export const ResumeIdContext = createContext(null)
export const TogglePreview = createContext(false)
export const ToggleExperience = createContext(false)

